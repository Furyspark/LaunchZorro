{
  "main": "main.js",
  "name": "launchzorro",
  "private": true,
  "scripts": {
    "test": "node compile.js && electron . debug",
    "test-cmd": "node compile.js && node app.js category=\"MMO\" profile=\"World of Warcraft\""
  },
  "devDependencies": {
    "concatenate-files": "^0.1.1",
    "electron-packager": "^8.0.0",
    "electron-prebuilt": "^1.3.5"
  },
  "dependencies": {
    "socket.io": "^1.7.2"
  }
}
